# 2026-09-22

- 完成 Unit04 学生名单多视图练习的三个延伸任务（my-vue-app-new/src/Student.vue）：分组视图 v-else-if 分支、只看及格 el-switch、el-dialog 详情框；main.js 注册 Element Plus 全量引入。
- 重大环境坑：本机 pnpm 创建 junction 系统性失败，node_modules 里大量空目录假链接（element-plus 装完不可用）。修复脚本 D:/first_one/.workbuddy/repair-nm.cjs：扫描 .pnpm 断链并用 node fs.symlinkSync(target,link,junction) 重建；注意 npm 别名依赖（@popperjs/core 是 @sxzz/popperjs-es 的别名）要手动指到真实包名。修复后删 node_modules/.vite 缓存再重启 dev。
- 验证：CDP 全部断言通过、控制台干净，6 张截图在 D:/first_one/.workbuddy/shots-student/。
